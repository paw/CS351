angular.
  module('storeApp').
  component('acme', {
    // Note: The URL is relative to our `index.html` file
    templateUrl: 'acme/acme.template.html',
    controller: function AcmeController() {
    }
  });